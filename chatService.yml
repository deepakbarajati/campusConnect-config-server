server:
  port: 8083
  servlet:
    context-path: /campusConnect/chat

spring:
  data:
    mongodb:
      uri: mongodb+srv://deepakbarajati2004_db_user:4llkudx5s0K8LsN5@campusconnnect.ufyx2h6.mongodb.net/campusConnect

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

userService:
  base:
    url: http://localhost:8081/campusConnect/user/

logging:
  level:
    com.campusConnect.chatService: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
